#!/bin/bash
KALTURA_BIN="$(cd "${0%/*}" 2>/dev/null; echo "$PWD")"
KALTURA_BIN_DIRS=$KALTURA_BIN/latest-dir
KALTURA_BIN_CURL=$KALTURA_BIN_DIRS/curl
LD_LIBRARY_PATH=$KALTURA_BIN_CURL $KALTURA_BIN_CURL/curl "$@"
